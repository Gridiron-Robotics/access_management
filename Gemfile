# Pins the Kamal deploy tool to an exact version so every machine/CI deploys
# with the same Kamal. Install with: bundle install. Use with: bundle exec kamal ...
source "https://rubygems.org"

gem "kamal", "2.11.0"
